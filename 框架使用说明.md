# 原型浏览器框架使用说明

## 简介

这是一个自定义的原型浏览器框架，用于浏览和管理项目中的所有原型页面。参考了 Axure 的设计理念，提供了类似的功能体验。

## 功能特性

✅ **左侧页面目录**
- 按分类展示所有原型页面
- 支持分类展开/收起
- 实时搜索过滤页面

✅ **顶部导航栏**
- 项目标识和 logo
- 需求说明查看按钮
- 侧边栏切换按钮

✅ **需求说明面板**
- 显示项目基本信息
- 页面统计信息
- 分类统计
- 使用说明

✅ **右侧内容展示**
- iframe 加载原型页面
- 响应式布局支持

## 文件结构

```
支付分账/
├── index.html                    # 框架入口（已替换）
├── framework.html               # 框架模板（备份）
├── axure_index.html             # 旧的 Axure 入口（备份）
├── resources/
│   ├── css/
│   │   └── framework.css        # 框架样式
│   └── scripts/
│       └── framework.js         # 框架脚本
├── 店铺后台_*.html              # 原型页面
├── 订单支付*.html                # 原型页面
└── ...                          # 其他原型页面
```

## 使用方法

### 启动框架

1. 打开 `index.html` 文件
2. 框架会自动加载默认页面（数据概览）

### 浏览页面

- **切换页面**：点击左侧菜单中的页面名称
- **搜索页面**：在搜索框输入关键词
- **展开/收起分类**：点击分类标题

### 查看需求说明

- 点击顶部导航栏的 **"📋 需求说明"** 按钮
- 查看项目信息、页面统计和使用说明

### 切换侧边栏

- 点击顶部导航栏的 **"☰"** 按钮
- 侧边栏会收起/展开，为内容区域提供更多空间

## 自定义配置

### 添加新页面

编辑 `resources/scripts/framework.js` 文件，在 `pageConfig.categories` 中添加新页面：

```javascript
categories: {
    '分类名称': {
        icon: '🏪',  // 分类图标
        pages: [
            {
                name: '页面名称',
                file: '页面文件.html',
                icon: '📄'  // 页面图标
            }
        ]
    }
}
```

### 修改默认页面

修改 `pageConfig.defaultPage` 的值：

```javascript
defaultPage: '你的默认页面.html'
```

### 修改分类顺序

调整 `categories` 对象中的键顺序即可。

## 特性说明

### 响应式设计
- 支持桌面端和移动端
- 自动适配不同屏幕尺寸

### 搜索功能
- 实时过滤页面
- 搜索时自动展开所有分类
- 支持页面名称和文件名搜索

### 状态保持
- 点击页面后左侧菜单会高亮显示
- 自动记住当前查看的页面

### 视觉效果
- 渐变色顶部导航栏
- 平滑的动画过渡
- 现代化的 UI 设计

## 注意事项

⚠️ **页面路径**：确保所有页面路径正确，框架会根据 `file` 属性加载页面

⚠️ **跨域问题**：如果通过 HTTP 服务器访问，确保页面在同一域下

⚠️ **浏览器兼容**：建议使用现代浏览器（Chrome、Firefox、Safari、Edge）

## 与 Axure 框架的关系

这个框架**独立于 Axure**，不依赖 Axure 的播放器组件，原因如下：

1. ✅ **更轻量**：仅使用原生 JavaScript 和 jQuery
2. ✅ **更灵活**：易于自定义和扩展
3. ✅ **更快速**：没有 Axure 的额外加载开销
4. ✅ **纯静态**：所有原型页面保持静态 HTML

如果需要使用 Axure 的交互功能，可以参考 `axure_index.html`（已备份）。

## 后续优化建议

- [ ] 添加页面书签功能
- [ ] 添加最近访问记录
- [ ] 支持页面分组和排序
- [ ] 添加暗黑模式
- [ ] 导出页面列表为文档

---

**版本**：v1.0.0
**更新日期**：2026-09-10
