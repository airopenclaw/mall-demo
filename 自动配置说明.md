# 自动配置说明

## 📊 当前状态

✅ **配置由扫描脚本自动生成，无需手工维护**

当前扫描结果（运行 `node scan_html.js` 后显示）：

- 2 个版本：支付分账、分账设置
- 5 个分类
- 19 个页面

> 增删页面后重新运行一次扫描脚本，上面的数字会随目录自动更新。

## ❓ 为什么不能自动加入菜单？

**技术限制：**

1. **浏览器安全策略**
   - JavaScript 运行在浏览器中，无法直接访问服务器文件系统
   - 防止恶意网站读取用户硬盘文件

2. **纯前端页面的限制**
   - 直接打开 `index.html` 走的是静态配置 `config.js`
   - 页面增删后需要重新生成配置

## 🔧 解决方案：扫描生成脚本

### 工作原理

- `scan_html.js` **复用 `server.js` 里的同一套扫描逻辑（`generateConfig()`）**，保证静态配置与服务器实时接口 `/api/config` 结构完全一致，不会出现一边更新、另一边过期。
- 扫描结果写入 **`resources/scripts/config.js`**——这是 `index.html` 真正加载的文件（`<script src="resources/scripts/config.js">`）。

### 两种使用方式

| 方式 | 命令 | 访问方式 | 特点 |
|------|------|----------|------|
| 服务器模式（推荐） | `node server.js` | http://localhost:3000 | 实时扫描，点「🔄 更新」即刷新，无需手动生成 |
| 静态模式 | `node scan_html.js` | 直接打开 `index.html` | 改完文件夹需重新运行脚本 |

### 使用方法

```bash
cd /Users/mac/Documents/原型HTML/mall

# 方式一：服务器模式（实时）
node server.js
# 访问 http://localhost:3000

# 方式二：静态模式（生成配置后直接打开 index.html）
node scan_html.js
```

### 输出

- 控制台显示扫描统计（版本数 / 分类数 / 页面数）
- 生成 `resources/scripts/config.js`

## 📁 目录结构规则

扫描按以下规则组织菜单，**不需要手写配置**：

1. **顶层内容文件夹 = 版本**
   只保留包含 HTML 的文件夹；`resources`、`data`、`files`、`images`、`plugins`、`prototype`、`axure`、`player`、`备份` 等框架/资源目录会被排除。
2. **子文件夹 = 分类**：文件夹下的 HTML 归入该分类。
3. **文件名前缀自动隐藏**：显示名会去掉「文件夹名_」前缀，例如 `店铺后台_仓库管理.html` 显示为「仓库管理」，但 `file` 路径保留完整相对路径。

当前目录结构：

```
mall/
  ├── resources/             # 框架/资源目录（被排除，不作为版本）
  ├── 支付分账/               # 版本 1（固定排在第一位）
  │   ├── 后端接口/           # 分类 🔌
  │   ├── 平台后台/           # 分类 ⚙️
  │   ├── 店铺后台/           # 分类 🏪
  │   └── 移动商城/           # 分类 📱
  └── 分账设置/               # 版本 2
      └── 平台后台/           # 分类 ⚙️
```

## 📝 如何添加新页面

### 方法 1：放入文件夹后用脚本扫描（推荐）

1. 将新的 HTML 文件放入对应分类文件夹，例如 `支付分账/店铺后台/`
2. 生成配置：
   - **服务器模式**：直接点页面顶部「🔄 更新」按钮（或刷新 http://localhost:3000）
   - **静态模式**：运行 `node scan_html.js`，然后硬刷新浏览器（Ctrl+Shift+R）

### 方法 2：手动编辑 config.js

1. 打开 `resources/scripts/config.js`
2. 在 `defaultPageConfig` 对应的版本/分类下添加：

```javascript
{ name: '新页面名称', file: '支付分账/店铺后台/新页面.html' }
```

> 注意：手动改过 `config.js` 后再运行 `scan_html.js`，内容会被自动生成的版本覆盖。

## ⚠️ 注意事项

1. **页面要放在内容文件夹内**：根目录散落的 HTML 不会被扫到（只有顶层内容文件夹才作为版本）。
2. **文件名即页面名**：显示名从文件名提取，自动去掉 `.html` 后缀及「文件夹名_」前缀。
3. **更新配置后**：服务器模式点「更新」或重启 `server.js`；静态模式重新运行 `scan_html.js` 并硬刷新浏览器。
4. **config.js 与 /api/config 共用同一套生成逻辑**：结构始终一致，单点维护。
