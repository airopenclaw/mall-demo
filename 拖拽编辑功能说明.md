# 拖拽与编辑功能使用指南

## ✨ 新功能介绍

### 1. 编辑模式

点击顶部导航栏的 **"✏️ 编辑"** 按钮进入编辑模式。

**编辑模式下可以：**
- ✅ 拖拽页面到不同版本/分类
- ✅ 编辑版本名称和描述
- ✅ 编辑分类名称和图标
- ✅ 新增/删除页面
- ✅ 新增/删除分类
- ✅ 新增/删除版本

**进入编辑模式后：**
- 顶部显示 **"✏️ 编辑"** 按钮变为激活状态
- 出现 **"📤 导出"** 按钮
- 左侧显示 **"➕"** 按钮（新增版本）
- 鼠标悬停在项目上时显示操作按钮

---

### 2. 拖拽页面

**操作步骤：**
1. 点击 **"✏️ 编辑"** 进入编辑模式
2. 鼠标左键按住要拖动的页面
3. 拖动到目标版本或分类
4. 松开鼠标完成移动

**拖拽规则：**
- ✅ 页面可以在不同版本之间移动
- ✅ 页面可以在同一版本的不同分类之间移动
- ✅ 拖动到版本上会自动放到该版本的第一个分类
- ✅ 拖动到分类上会放到该分类的末尾
- ✅ 拖动到同一位置会提示"页面已在当前位置"

**视觉反馈：**
- 拖拽中的元素会半透明显示
- 目标位置会显示绿色虚线边框
- 底部会显示拖拽提示信息

---

### 3. 编辑版本

**操作步骤：**
1. 进入编辑模式
2. 将鼠标悬停在版本标题上
3. 点击出现的 **"✏️"** 按钮
4. 修改版本名称和描述
5. 点击 **"保存"** 或 **"删除"**

**编辑功能：**
- ✅ 修改版本名称
- ✅ 修改版本描述
- ✅ 删除版本（会删除该版本下所有页面）
- ✅ 自动检测重名

---

### 4. 编辑分类

**操作步骤：**
1. 进入编辑模式
2. 将鼠标悬停在分类标题上
3. 点击出现的 **"✏️"** 按钮
4. 修改分类名称
5. 点击 **"保存"** 或 **"删除"**

**编辑功能：**
- ✅ 修改分类名称
- ✅ 删除分类（会删除该分类下所有页面）
- ✅ 自动检测重名

---

### 5. 新增页面

**操作步骤：**
1. 进入编辑模式
2. 将鼠标悬停在分类标题上
3. 点击出现的 **"➕"** 按钮
4. 输入页面名称和文件名
5. 点击 **"添加"**

**注意：**
- 文件名需要是实际存在的 HTML 文件
- 会自动添加 `.html` 后缀（如果未输入）
- 重复的文件名会被拒绝

---

### 6. 导出配置

**操作步骤：**
1. 进入编辑模式
2. 点击顶部 **"📤 导出"** 按钮
3. 选择导出方式：
   - **📋 复制到剪贴板** - 直接复制配置代码
   - **💾 下载配置文件** - 下载为 JS 文件

**配置代码示例：**
```javascript
// 页面数据配置
const pageConfig = {
    versions: {
        'V1.0 - 基础版': {
            icon: '🎯',
            description: '店铺后台与订单支付基础功能',
            categories: {
                // ... 分类和页面
            }
        }
    },
    defaultPage: '店铺后台_数据概览.html'
};
```

**如何使用导出的配置：**
1. 复制或下载配置代码
2. 打开 `resources/scripts/framework.js`
3. 替换 `defaultPageConfig` 对象的内容
4. 保存文件并刷新页面

---

## 💾 数据持久化

### localStorage 存储

所有修改都会自动保存到浏览器的 `localStorage` 中：

- **键名**: `prototypePageConfig`
- **存储内容**: 完整的页面配置（包括版本、分类、页面）
- **生命周期**: 永久保存（除非清除浏览器数据）

### 清除保存的配置

如果需要恢复默认配置，可以在浏览器控制台运行：

```javascript
localStorage.removeItem('prototypePageConfig');
location.reload();
```

---

## 🎯 使用场景

### 场景 1: 调整页面结构

**需求**：将"订单支付"页面从 V1.0 移动到 V1.2

**操作**：
1. 进入编辑模式
2. 拖拽"订单支付"页面到 V1.2 > 交易流水分类
3. 完成！配置自动保存

### 场景 2: 添加新功能版本

**需求**：新增一个 V2.0 版本

**操作**：
1. 点击顶部 **"➕"** 按钮
2. 编辑版本名称和描述
3. 在该版本下新增分类和页面
4. 点击 **"📤 导出"** 保存配置到代码

### 场景 3: 重命名分类

**需求**：将"店铺后台"改名为"商家管理后台"

**操作**：
1. 进入编辑模式
2. 悬停在"店铺后台"分类上
3. 点击 **"✏️"** 按钮
4. 修改名称为"商家管理后台"
5. 点击保存

---

## ⚠️ 注意事项

### 拖拽限制

1. **只能拖拽页面**，不能拖拽版本或分类
2. **必须进入编辑模式**才能拖拽
3. **拖拽后自动保存**，无法撤销（可通过恢复默认配置重置）

### 编辑限制

1. **删除版本**会删除该版本下的所有分类和页面
2. **删除分类**会删除该分类下的所有页面
3. **重命名**时不能与现有名称重复
4. **新增页面**需要文件实际存在

### 数据安全

1. ✅ 所有修改仅保存在浏览器本地
2. ✅ 不会修改任何原始 HTML 文件
3. ✅ 刷新页面后修改依然存在
4. ⚠️ 清除浏览器数据会丢失自定义配置

---

## 🔧 故障排除

### 拖拽不工作

**可能原因：**
- 未进入编辑模式
- 浏览器不支持 HTML5 拖拽 API

**解决方案：**
1. 点击顶部 **"✏️ 编辑"** 按钮
2. 确保使用现代浏览器（Chrome、Firefox、Safari、Edge）

### 修改丢失

**可能原因：**
- 清除了浏览器缓存
- 使用了隐私/无痕模式

**解决方案：**
1. 导出配置并保存到代码
2. 定期导出配置备份

### 页面加载失败

**可能原因：**
- 拖拽后页面文件不存在
- 文件路径错误

**解决方案：**
1. 检查页面文件是否真的存在
2. 重新导出默认配置恢复

---

## 📝 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Esc` | 关闭当前模态框 |
| `Ctrl/Cmd + C` | 复制配置代码（在导出模态框中） |

---

**版本**: v1.2.0
**更新**: 2026-09-10
**新特性**: ✅ 拖拽管理 | ✅ 编辑模式 | ✅ 数据持久化 | ✅ 配置导出
